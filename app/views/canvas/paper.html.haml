- content_for :head do
  %script{ :type => 'text/paperscript', :src => javascript_path('ps/brush.js'), :canvas => 'brush'}

%h1 Paper

#paper
  %canvas{ :id => 'brush', :width => '800', :height => '800' }
